apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: authentication
          env:
            - name: ENV
              value: "production"

