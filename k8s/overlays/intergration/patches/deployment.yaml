apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: authentication
          env:
            - name: ENV
              value: "intergration"
